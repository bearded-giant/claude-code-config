Add a note to the workspace.

Append the user's note to the `## Notes` section of `.giantmem/WORKSPACE.md`.

Format: `- YYYY-MM-DD HH:MM: {note content}`

If .giantmem/WORKSPACE.md doesn't exist, tell the user to run /ws-init first.

User's note: $ARGUMENTS
